@typeparam TItem

@if(ListOfItems == null)
{
    if (NullList != null)
    {
        @NullList
    }
    else
    {
        <img src="/pages/RenderContent/NullList.gif" height="200" />
    }
}
else if (ListOfItems.Count == 0)
{
    if (EmptyList != null) 
    {
        @EmptyList
    }
    else
    {
        <img src="/pages/RenderContent/EmptyList.gif" height="200" />
    }
}
else
{
    @foreach (var item in ListOfItems)
    {
        @ItemTemplate(item)
    }
}

@code {
    [Parameter] public RenderFragment NullList { get; set; }
    [Parameter] public RenderFragment EmptyList { get; set; }
    [Parameter] public RenderFragment<TItem> ItemTemplate { get; set; }
    [Parameter] public List<TItem> ListOfItems { get; set; }
}
